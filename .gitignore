# Source dataset (CC0; kept out of git to avoid a redundant copy — see docs/dataset-license.md)
data/raw/

# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/

# Launcher (TypeScript)
launcher/node_modules/
launcher/dist/
data/.mint-keys/
data/fee-config.json
data/launch-alt.json

# Secrets — never commit (belt-and-suspenders)
*.key
*.pem
*.keypair.json
.env
.env.*
!.env.example

# Web (Next.js)
web/node_modules/
web/.next/
web/out/
web/public/token_images/
web/next-env.d.ts
web/*.tsbuildinfo

# Local — not for the public repo
/CLAUDE.md
/pumptank.zip
/ChatGPT Image*.png
